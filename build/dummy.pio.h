// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ----- //
// dummy //
// ----- //

#define dummy_wrap_target 0
#define dummy_wrap 1
#define dummy_pio_version 0

static const uint16_t dummy_program_instructions[] = {
            //     .wrap_target
    0x80a0, //  0: pull   block
    0x6004, //  1: out    pins, 4
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program dummy_program = {
    .instructions = dummy_program_instructions,
    .length = 2,
    .origin = -1,
    .pio_version = dummy_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config dummy_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + dummy_wrap_target, offset + dummy_wrap);
    return c;
}

static inline void dummy_program_init(PIO pio, uint sm, uint offset, uint pin) {
    pio_sm_config c = dummy_program_get_default_config(offset);
    // Map the state machine's OUT pin group to 4 pins, starting from the 'pin' parameter
    sm_config_set_out_pins(&c, pin, 4);
    // Set this pin's GPIO function (connect PIO to the pad)
    for (int i = 0; i<4; i++)
    	pio_gpio_init(pio, pin+i);
    // Set the pin direction to output at the PIO
	pio_sm_set_consecutive_pindirs(pio,sm,pin,4,true);
    // Load our configuration, and jump to the start of the program
    pio_sm_init(pio, sm, offset, &c);
    // Set the state machine running
    pio_sm_set_enabled(pio, sm, true);
}

#endif

